# Python outside the sidecar and packages (examples/, scripts): same style (CLAUDE.md §8).
line-length = 100
target-version = 'py310'

[format]
indent-style = 'tab'
quote-style = 'single'

[lint]
select = ['E', 'F', 'W', 'I', 'B', 'UP', 'S', 'RUF', 'N']
ignore = ['W191', 'E101']
