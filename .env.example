# Forge does NOT read API keys from .env. Secrets live in Settings → Secrets (encrypted with safeStorage).
# This file only documents optional developer overrides.

# Log level for main process (error | warn | info | debug)
FORGE_LOG_LEVEL=info

# Set to 1 to skip auto-starting the Python sidecar in dev
FORGE_NO_SIDECAR=0
